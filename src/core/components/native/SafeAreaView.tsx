import {styled} from '@gluestack-style/react';
import {SafeAreaView as NativeSafeAreaView} from 'react-native';

const SafeAreaView = styled(NativeSafeAreaView, {flex: 1});

export default SafeAreaView;
